"use client";

import { AdminProperties } from "@/components/dashboard/admin-properties";

export default function AdminListingsPage() {
  return (
    <AdminProperties
      title="Annuaires"
      subtitle="Vue des annonces de la plateforme (alias des biens)."
    />
  );
}
